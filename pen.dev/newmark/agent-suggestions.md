# Предложения по улучшению

## По гайдам

1. **css.md пуст** — либо удалить, либо наполнить. В проекте используются SCSS, но пустой файл в guides/ может ввести в заблуждение.

2. **Нет гайда по дизайн-токенам** — в макете используются:
   - Цвета: оранжевый акцент (#d97757), тёмный фон, белый текст
   - Шрифты (в макете не указаны, но по контексту — без засечек)
   - Отступы, радиусы
   - Хорошо бы добавить `abstracts/_tokens.scss` с ними

## По HTML-верстке

3. **Изображения** — реальные изображения из `design/images/` подключены в `index.html`. Если для продакшна потребуется оптимизация, стоит отдельно подготовить webp/avif-версии и правила responsive images.

4. **Счётчик в CTA** — в макете статические значения (02:14:36:08). Для продакшна нужен JS-таймер, ведущий обратный отсчёт. Пока оставлено как статика.

5. **Бургер-меню** — на мобильных экранах навигация не поместится. В макете десктопная версия (1440px). Для mobile-first нужна мобильная адаптация навигации.

## Технические

6. **SCSS-архитектура** — для проекта хорошо подойдёт структура из гайда `scss.md`. Рекомендую сразу создать каркас:
   - abstracts/ (_variables, _tokens, _mixins)
   - base/ (_reset, _fonts, _global, _typography)
   - layout/ (_container, _section, _header, _footer, _grid)
   - components/ (_button, _logo, _nav, _product-card, _feature-card, _benefit-card, _section-header, _form, _field, _cta, _catalog-group, _print-card, _accent-line, _social-list, _link-list, _file-button)
   - pages/ (_home)

7. **Оранжевая линия-акцент** — в макете используется как декоративный элемент перед заголовками (`Why Benefit Title Top Orange Line`). В HTML сделано через `benefit-card__accent`. Уточнить название.

8. **Hero текст** — текст перенесён из `.pen` файла и приведён к обычному регистру по гайдам.
