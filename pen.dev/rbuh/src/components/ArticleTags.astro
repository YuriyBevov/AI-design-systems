---
const { tags } = Astro.props;
---
<ul class="tag-list" aria-label="Теги материала">
  {tags.map((tag) => (
    <li><a class="tag" href={`/information/?tag=${encodeURIComponent(tag)}`}>{tag}</a></li>
  ))}
</ul>
