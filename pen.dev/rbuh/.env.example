NODE_ENV=production
HOST=127.0.0.1
PORT=4321

# Fill in SMTP credentials on the server only. Never commit the .env file.
SMTP_HOST=
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
SMTP_TO=
